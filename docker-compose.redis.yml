version: '3.8'

services:
  # Redis Cache
  redis:
    image: redis:7
    container_name: WhoWillAnswer_mariadb
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data: